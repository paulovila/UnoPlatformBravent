<Page
    x:Class="UnoWebApiSwagger.Shared.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoWebApiSwagger.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:UnoWebApiSwagger.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DataContext="{d:DesignInstance viewModels:LoginViewModel}">

    <Grid VerticalAlignment="Center" Width="400" Background="LightGray" Padding="30 20 30 0" >

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0">
            <TextBlock Style="{StaticResource HeaderTextBlockStyle}" TextAlignment="Center" Text="Login" />
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Please, log in" Margin="0 10" />

            <StackPanel>
                <TextBox Header="Username" Text="{Binding UserName}" />
                <TextBox Header="Password" Text="{Binding Password}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20">
            <Button Content="Login" Command="{Binding LoginCommand}"  />
        </StackPanel>


    </Grid>


</Page>
